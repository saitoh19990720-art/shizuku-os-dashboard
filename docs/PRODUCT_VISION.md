# PRODUCT_VISION.md｜Shizuku OS / Shizuku Studio

> これは**実装指示書ではない**。Shizuku OS の思想・方向性・壊してはいけない価値を残す**北極星メモ**。
> 実装順は `ROADMAP.md`、起動方法は `README.md`。
> 実装ルールと見た目のルールはこのリポジトリには置いていない（ワークスペース側の `.claude/rules/` が正本。実装した色・フォントは `tailwind.config.js`）。
> 最終更新：2026-07-01

> **読むときの注意（2026-08-30 追記）**
> **§4「v0.1 のスコープ」は当時の記録であり、現在の姿ではない。** 本文は原則として書き換えない（何を最初のスコープに決めたかという判断履歴を残すため）。
> ただし1点だけ例外がある：モバイル幅の表記を 2026-08-30 に実装事実へ修正した（旧「スマホ幅（360px前後）」→ 現行表記）。360px は当時の要件ではなく監査時の測定用ビューポートで、要件のように読めてしまうため。**スコープの判断内容そのものは変えていない。**
> 現在の実装は **12カード**。最新の機能一覧は `README.md`、実装の順番は `ROADMAP.md` が正本。
> §1〜§3・§6（思想・中核・壊してはいけない価値）は今も有効。

---

## 1. これは何か

Shizuku OS は、単なるタスク管理アプリではない。
しずくの **制作・判断・記録・再開** を、1画面で静かに支える**個人用の制作OS**。

「便利ツール」ではなく、しずくの仕事の仕方そのものを形にしたもの。
だから、機能の多さより「**毎日ここに戻ってこられるか**」を優先する。

---

## 2. なぜ作るのか（核の思想）

```txt
AIで速く作る → AIっぽさを消す → 人間の判断と美意識で「仕事になる形」に戻す。
```

AIに全部任せて終わりにしない。
**AIの速さは使う。でも、何を採用し・何を捨て・どう仕上げるかは人間が決める。**
その「速さ→仕事品質」への変換を、毎日の習慣として回す装置が Shizuku OS。

中核にある3つの価値：

1. **迷わず戻れる** — 昨日の続きに、考えずに戻れる
2. **雑な案を通さない** — 思いつきをそのまま採用しない（Quality Gate）
3. **AIの速さを仕事品質へ変える** — 速いだけで終わらせない

---

## 3. 中核コンポーネント（Core Components）

| # | カード | 役割 |
|---|---|---|
| 1 | 今日の制作候補 | 今日いちばん進める「1つ」を決める・見失わない |
| 2 | 夜タスク3行ログ | やった／学び／次やる。1日を3行で沈殿させる |
| 3 | 制作中リンク | Figma・GitHub・メモ・公開URLを置いて、再開を速くする |
| 4 | **Quality Gate** | 案・制作物を「採用／保留／捨てる」で判定する |

### Quality Gate は Shizuku OS の中核

ここが Shizuku OS を「タスク管理」から「**制作OS**」に変える心臓部。
4つの視点（**部下視点／消費者視点／勝算／安全**）で案を見て、雑なまま前に進めない。
合言葉：**雑案を増やすな。最終判断はしずく。**

---

## 4. v0.1 のスコープ

### やること（v0.1）
- 4カード構成（今日の制作候補／夜タスク3行ログ／制作中リンク／Quality Gate）
- localStorage 保存（この端末内だけ・外部送信なし）
- スマホ幅を基準にした1カラムの静かなUI（最大400px程度・320pxまで破綻しない）
- React + TypeScript + Tailwind + README まで
- ローカルで動く（`npm run dev`）ことが合格ライン

### やらないこと（v0.1）
- 外部API連携
- ログイン／認証
- n8n 本接続
- GitHub 自動連携
- 課金
- 通知

> 未来構想を v0.1 に混ぜない。まず「迷わず戻れる4カードがローカルで動く」を完成にする。

---

## 5. ドキュメントの役割分担

| ファイル | 役割 |
|---|---|
| `docs/PRODUCT_VISION.md` | **思想**（このファイル・北極星） |
| `docs/ROADMAP.md` | 実装の順番 |
| `docs/FEEDBACK_LOOP.md` | 公開後の反応を次へ戻す手順 |
| `README.md` | 起動方法・機能・構成 |
| `tailwind.config.js` | 実装された配色・フォントのトークン |

> `CLAUDE.md` / `STYLE_GUIDE.md` は**このリポジトリには無い**。
> AIの作業ルールとデザインの正本はワークスペース側の `.claude/rules/`
> （`design-system.md` / `accessibility.md` / `ui-patterns.md`）にある。

---

## 6. 壊してはいけない価値（Do Not Break）

- **静けさ**：量産SaaSダッシュボードの賑やかさにしない
- **戻れること**：始めるためだけでなく、戻ってくるための画面
- **人間が決める**：AIは速さと整理。採用・方向・公開の最終判断はしずく
- **しずくらしさ**：冷色・水色・透明感・明朝。淡色のまま、形だけ少し中性に
- **小さく完成させる**：機能を増やすより、今あるものを「作」にする
