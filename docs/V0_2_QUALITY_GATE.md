# V0_2_QUALITY_GATE.md｜v0.2候補を Quality Gate に通す

> 「全部やる」ではなく、採用／保留／捨てる で次の一手を1つに絞る。
> v0.2で採用するのは**最大1つ**。最終判断はしずく。最終更新：2026-07-01

---

## 候補① Quality Gate 履歴
- **部下視点**：実装負荷=小（localStorage配列に追加するだけ）／壊れにくい／保守軽い
- **消費者視点**：分かりやすい・「判断が積み上がる」体験で再開しやすい
- **勝算**：Shizuku OSの**中核（採用判断）そのもの**を強化＝ポートフォリオ価値が高い・次にも使える
- **安全**：個人情報なし／外部連携なし／戻せる（localStorageのみ）
- **判定：採用** ✅
- **次の一手（小さく）**：判定時に `{案名, verdict, 日付}` を履歴配列に保存し、Quality Gateカードの下に新しい順で一覧表示。削除も可。

## 候補② Obsidian向け Markdown 出力
- **部下視点**：実装負荷=中（出力フォーマット設計＋コピー/DL導線）
- **消費者視点**：刺さる人には有用だが、今すぐ全員に要るわけではない
- **勝算**：Obsidian導線（§8）と相性◎だが、v0.2の主役にするには重め
- **安全**：localStorage→テキスト出力なので安全
- **判定：保留** 🟡（v0.3候補。①の履歴ができてから「履歴をMarkdownで出す」と自然につながる）

## 候補③ UI微調整・スマホ表示改善
- **部下視点**：実装負荷=小だが範囲が曖昧になりやすい
- **消費者視点**：効くが地味（44px化は済・空状態など細部）
- **勝算**：単体ではポートフォリオの目玉になりにくい
- **安全**：安全
- **判定：保留（随時）** 🟡（v0.2の主役にせず、気づいた時に小さく直す扱い）

---

## 結論
- **v0.2で採用：① Quality Gate 履歴**（最大1つの枠はこれ）
- 保留：② Obsidian出力（→v0.3）／③ UI微調整（→随時）
- **大きな外部連携・n8n本接続はしない**（v0.2スコープ外）

## まだ実装しない
このファイルは**評価まで**。実装はしずくが「①をv0.2として作る」と判断してから。
