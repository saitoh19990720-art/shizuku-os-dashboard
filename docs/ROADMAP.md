# ROADMAP.md｜Shizuku OS Dashboard

> 「全部やる」ではなく Quality Gate で 採用／保留／捨てる を決める。
> 最終更新：2026-08-30（v0.11時点の内容から実態へ更新）

---

## 現在地

| | |
|---|---|
| 公開中 | **v0.18・12カード**（公開URLへ反映済み・2026-08-30 実測で確認） |
| ローカル | **v0.19**（表記の事実整合のみ・未デプロイ） |
| ビルド | `npm run build` 成功 |
| 外部連携 | なし（localStorage のみ・外部送信なし） |

---

## 実装済み（v0.1〜v0.18）

今日のコンディション / 今日の次アクション / 今日の制作候補 / AI Role Router / Prompt Builder＋Vault /
夜タスク3行ログ / 制作中リンク / Quality Gate（履歴・Obsidian出力・絞り込み）/ Weekly Review /
Brand Panel / n8n Bridge（JSON入出力・型検証・失敗時ロールバック）/ 今日はここまででOK
＋ Landing ＋ ハッシュルーティング ＋ アクセシビリティ仕上げ。GitHub・Vercel 公開済み。

> **旧ROADMAPで「v0.12で採用」としていた カード折りたたみ／Weekly Review は、どちらも実装完了**（v0.12・v0.13）。
> ChatGPT提案の AI Crew Panel＝Role Router／Prompt Transformer＝Prompt Builder+Vault／n8n Bridge も実装済みのため候補から除外。

---

## 既知の未解決（記録・まだ直さない）

### 1. 二次テキストのコントラストが AA 未達 🔴

| | |
|---|---|
| 対象 | `text-neutral2-300` = **`#D9D9D9`**（`tailwind.config.js` の `neutral2.300`） |
| 実測 | 白背景でのコントラスト比 **約 1.4:1**（WCAG AA の本文基準は 4.5:1） |
| 範囲 | **`src/` 内 47か所**。カードの説明文・補足文・空状態メッセージがほぼ全部これ |
| 具体例 | 「制作・夜ログ・リンク・採用判定を、静かに一画面で。」／「体調を無視しない。今日できる分だけ、静かに進める。」 |
| 矛盾点 | `.claude/rules/accessibility.md` は本文 4.5:1 を必須にしており、README v0.8 は「アクセシビリティ仕上げ」と書いている。**主張と実装が食い違っている** |
| 経緯 | v0.18 の「やっていないこと」にも `neutral2-300 の色変更` と明記済み＝**既知の未対応** |

**判断（2026-08-30・本人）：今は一括置換しない。**
理由 — **Figma が色の正本**であり、Figma 側の確認なしに47か所をまとめて変えない。

**直すときの最小案（未承認・参考）**
`neutral2` に 400番（例：`#8A9099` 前後＝白背景で 4.5:1 超）を1つ追加し、**文字用途だけ**を差し替える。
枠線・背景・区切り線の淡色（`neutral2-200` など）はそのまま。淡色の静けさは保てる。
→ **着手条件：Figma 側で二次テキスト色が確定すること。**

### 2. スクリーンショットが古い

`docs/images/screenshot-dashboard.png` は **v0.17（11カード）時点**で、「今日の次アクション」が写っていない。
さらにビューポート撮影のため**カード1.5枚しか入っておらず**、README の「カード一覧」という説明と合わない。
→ しずくが `#/dashboard` のページ全体を撮り直して差し替える。

### 3. Figma の正本URLがリポジトリに無い

`figma.com` / node-id の参照が**1件も無い**（全文検索で確認）。
説明文は「Figma で設計し」と書いているが、第三者が検証できない。
→ file URL と主要フレームの node ID を README に1行記録する。**しずくしか出せない情報。**

---

## 次の候補と Quality Gate 判定

| 候補 | 部下(実装) | 消費者(分かりやすさ) | 勝算 | 安全 | 判定 |
|---|---|---|---|---|---|
| **12カード → 6構造へ畳む**（North Star / Current / Bottleneck / Decisions / AI Office / Resume） | 重 | 高（縦に伸び続ける画面が整う） | **高**（作品の主張＝「複雑を静かに構造化」の実証） | ○ | **採用①（次の主テーマ）** |
| **Bottleneck カード**（詰まっていること・判断待ち） | 中 | 高 | **高**（他のタスクツールが持たない差別化点） | ○ | **採用②** |
| データを `projectId` で串刺しにする | 中 | 中 | 高（構造化の土台） | ○ | **採用③（①の前提）** |
| テスト基盤（Vitest）の導入 | 軽〜中 | — | 中（構造変更の保険。`.claude/rules/testing.md` 準拠） | ○ | **採用④（①の前に）** |
| 二次テキストの色修正 | 軽 | 高 | 高 | ○ | **保留（Figma確認待ち）** |
| meta description / OGP / favicon | 軽 | 中 | 中（応募でURLを貼った時に効く） | ○ | 保留（次の小さな一手） |
| 横断 Search / Filter | 中 | 中〜高 | 中 | ○ | 保留（データが増えてから） |
| AI 夜勤キュー | 中 | 中 | 中 | **要注意** | 保留（**自動実行にしない**。下書きキューとしてのみ） |
| 外部API / n8n 本接続 | 重 | — | — | **要注意** | **今はしない**（§11） |

## 実装順（次フェーズ）

```
1. 表記の事実整合（v0.19・完了）
2. テスト基盤 → 3. データモデル移行 → 4. Figmaで6構造の情報設計（ここで一度止まる）
→ 5. Current / Resume 統合 → 6. Bottleneck → 7. Decisions → 8. AI Office → 9. North Star + Landing
```

## 方針

```
v0.1〜0.18：動く・機能がそろう（済）
v0.19     ：説明と実物を一致させる（済）
次         ：機能を足さず、12枚を6構造へ畳む
将来       ：自動化につなぐ（n8n本接続＝承認後）
```

外部連携は急がない。中心は常にしずくの判断。
